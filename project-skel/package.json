{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{name}}: A rukus/riot web application",
  "main": "index.js",
  "scripts": {
    "test": "mocha --compilers js:babel/register --bail --check-leaks --recursive ./components/",
    "start" : "./node_modules/.bin/webpack-dev-server"
  },
  "author": "",
  "license": "",
  "dependencies": {
  }
}
